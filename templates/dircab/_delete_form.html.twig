<form method="post" action="{{ path('dircab_delete', {'id': dircab.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ dircab.id) }}">
    <button class="btn">Delete</button>
</form>
