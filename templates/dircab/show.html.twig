{% extends 'base.html.twig' %}

{% block title %}Dircab{% endblock %}

{% block body %}
    <h1>Dircab</h1>
<div class="row">
<div class="col-md-6">
    <table  class="table table-bordered">
     
        <tbody class="thead-dark">
            <tr>
                <th>Id</th>
                <td>{{ dircab.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ dircab.Nom }}</td>
            </tr>
            <tr>
                <th>Prenoms</th>
                <td>{{ dircab.Prenoms }}</td>
            </tr>
            <tr>
                <th>Date de nomination</th>
                <td>{{ dircab.DateNomin }}</td>
            </tr>
            <tr>
                <th>Arrete de Nomination</th>
                <td>{{ dircab.ArreteNomin }}</td>
            </tr>
            <tr>
                <th>Date d'Abrogation</th>
                <td>{{ dircab.DateAbrog ? dircab.DateAbrog|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Arrete d'Abrogation</th>
                <td>{{ dircab.ArreteAbrog }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ dircab.titre }}</td>
            </tr>
            <tr>
                <th>Phone</th>
                <td>{{ dircab.Phone }}</td>
            </tr>
            <tr>
                <th>Observation</th>
                <td>{{ dircab.observation }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('dircab_index') }}">Retour à la liste</a></br>

    <a href="{{ path('dircab_edit', {'id': dircab.id}) }}">editer</a>

    {{ include('dircab/_delete_form.html.twig') }}
    </div >
</div>
{% endblock %}
